import { StubMatRowDefDirective } from './stub-mat-row-def.directive';

describe('StubMatRowDefDirective', () => {
  it('should create an instance', () => {
    const directive = new StubMatRowDefDirective();
    expect(directive).toBeTruthy();
  });
});
