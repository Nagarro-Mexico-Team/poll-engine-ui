import { StubMatHeaderRowDefDirective } from './stub-mat-header-row-def.directive';

describe('StubMatHeaderRowDefDirective', () => {
  it('should create an instance', () => {
    const directive = new StubMatHeaderRowDefDirective();
    expect(directive).toBeTruthy();
  });
});
